export type DigitalRainMode = 'binary' | 'hex' | 'mixed';

export type DigitalRainColumn = {
    x: number;
    y: number;
    speed: number;
    chars: string[];
    length: number;
    life: number;
};
